<p><%= total %>개의 글</p>
<% if (!posts.length) { %><p>조건에 맞는 글이 없습니다.</p><% } %>
<% for (const post of posts) { %><%- partial('post-row', { post }) %><% } %>
<%- partial('pagination', { pager, pageUrl }) %>
